# SSR runtime configuration
SSR_HTTP_TIMEOUT_MS=5000
SSR_HTTP_RETRY_COUNT=1
SSR_WIDGET_FALLBACK=skeleton
SSR_PAGE_TIMEOUT_MS=10000

# Example API keys (replace with real values in deployment envs)
API_BASE_URL=
