{
  "buildCommand": "nx build member-client --configuration=production",
  "outputDirectory": "dist/member-client/browser",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
